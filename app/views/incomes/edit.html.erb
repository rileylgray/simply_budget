<% content_for :title, "Edit Income" %>

<div class="container mt-5" style="max-width: 600px;">
  <div class="card shadow">
    <div class="card-body">
      <h2 class="card-title mb-4 text-center">Edit Income</h2>
      <%= render "form", income: @income %>
      <div class="text-center mt-3">
        <%= link_to "Show this Income", @income, class: "btn btn-outline-primary me-2" %>
        <%= link_to "Back to Incomes", incomes_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>