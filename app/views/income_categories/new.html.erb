<% content_for :title, "New Income Category" %>

<div class="container mt-5" style="max-width: 600px;">
  <div class="card shadow">
    <div class="card-body">
      <h2 class="card-title mb-4 text-center">Add New Income Category</h2>
      <%= render "form", income_category: @income_category %>
      <div class="text-center mt-3">
        <%= link_to "Back to Categories", income_categories_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>